name: Test GitHub Actions Recognition

# 只手动触发，用于测试GitHub是否能识别这个文件
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test recognition
      run: |
        echo "🎉 GitHub Actions 成功识别了这个workflow文件！"
        echo "📂 文件路径: .github/workflows/test-recognition.yml"
        echo "✅ 如果你看到这条消息，说明GitHub正确识别了workflow文件"
